

<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.ETask" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/secondary</item> <!-- Color primario -->
        <item name="colorPrimaryVariant">@color/secondaryVariant</item> <!-- Color primario (tono menor o mayor) -->
        <item name="colorOnPrimary">#C1CDF3</item> <!-- Fuente del color primario -->
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/primary</item>
        <item name="colorSecondaryVariant">@color/primaryVariant</item>
        <item name="colorOnSecondary">@color/primaryOn</item>
        <!-- Status bar color. -->
        <item name="background">@color/background</item>
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

    </style>

    <style name="Theme.ETask.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.ETask.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.ETask.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>